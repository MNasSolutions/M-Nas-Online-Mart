// Legacy script - Knack integration moved to secure server-side proxy
// This file is no longer used for Knack API calls
// The KnackProducts React component now uses the secure edge function

document.addEventListener("DOMContentLoaded", () => {
    console.log("Legacy script.js loaded - Knack API calls are now handled securely via edge function");
});
